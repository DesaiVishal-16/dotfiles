# Reload configuration with `C-a + r`
unbind r
bind r source-file ~/.tmux.conf

# Change prefix key to Ctrl-A
set -g prefix C-a

# Enable mouse support
set -g mouse on

# Enable vi mode in tmux
setw -g mode-keys vi


# Pane numbering start from 1
set -g base-index 1
set-window-option -g pane-base-index 1

# Better terminal compatibility
set -g default-terminal "tmux-256color"

# Pane navigation shortcuts (Vim-style)
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Status bar position
set-option -g status-position top

# Set Catppuccin theme preferences
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "rounded"

# Customize the status line
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

# List of plugins (TPM-managed)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux#v2.1.1'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-battery'

# Initialize TPM and plugins (keep this line at the very bottom)
run '~/.tmux/plugins/tpm/tpm'


# tmux-resurrect
set -g @plugin 'tmux-plugins/tmux-resurrect'
run '~/.tmux/plugins/tmux-resurrect/resurrect.tmux'

#24 bit color

set-option -g terminal-overrides 'xterm-256color:Tc'

